<section id="calculator" class="calc-section">
  <div class="section-inner">
    <div class="calc-section-head" data-reveal>
      <span class="eyebrow">{{ __('messages.calc_eyebrow') }}</span>
      <h2 class="display-2 mt-2">{{ __('messages.calc_title') }}</h2>
      <p class="lead mt-5">{{ __('messages.calc_blurb') }}</p>
      <a href="#start" class="btn btn-primary mt-8" style="display:inline-flex;align-items:center;gap:10px">
        {{ __('messages.cap_promo_open') }}
        <i data-lucide="calculator"></i>
      </a>
      <p class="mt-4" style="font-size:14px;font-weight:600;color:var(--ink-600)">{{ __('messages.calc_capacity_approx') }}</p>
    </div>
  </div>
</section>
